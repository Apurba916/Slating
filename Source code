#include <stdio.h>
#include <string.h>
int main(){
    char password[100];
    printf("Enter your password:\n");
    fgets(password, 100, stdin);
    char salt[4] = "123";
    strcat(password, salt);
    puts(password);
    return 0;
}
